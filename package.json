{
  "name": "opaque",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://ryanrabello.github.io/opaque/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "yarn build && touch dist/.nojekyll && git add -f dist/** && git commit -m \"Deploy gh-pages\" && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "@types/d3": "^7.4.0",
    "@types/three": "^0.140.0",
    "d3": "^7.4.4",
    "simplex-noise": "^3.0.1",
    "three": "^0.140.2"
  }
}
